services:
  lemono:
    build: .
    ports: ["8000:8000"]
    env_file: [.env]
    volumes: ["lemono-data:/data"]
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
volumes:
  lemono-data:

