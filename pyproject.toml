[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lemono-agent"
version = "0.1.0"
description = "Local-first, self-diagnosing personal AI agent with durable memory and multi-model routing"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "fastapi>=0.115,<1",
  "pydantic>=2.8,<3",
  "uvicorn[standard]>=0.30,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9", "ruff>=0.6,<1"]

[project.scripts]
lemono = "lemono.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/lemono"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
